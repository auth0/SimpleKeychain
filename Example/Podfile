source 'https://github.com/CocoaPods/Specs.git'

target 'A0SimpleKeychain', :exclusive => true do
  pod "A0SimpleKeychain", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "A0SimpleKeychain", :path => "../"

  pod 'Specta', '~> 0.2.1', :inhibit_warnings => true
  pod 'Expecta'
end
